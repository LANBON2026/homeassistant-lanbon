set_channel_name:
  name: Set channel name
  description: Rename a LANBON switch channel on the panel (writes back to device).
  fields:
    entity_id:
      name: Entity
      description: LANBON switch entity
      required: true
      selector:
        entity:
          integration: lanbon
          domain: switch
    name:
      name: Name
      description: New button name
      required: true
      selector:
        text:
